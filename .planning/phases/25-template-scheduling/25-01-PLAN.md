---
phase: 25-template-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260201000001_add_template_task_scheduling.sql
  - src/integrations/supabase/types.ts
  - src/hooks/useTemplates.ts
autonomous: true

must_haves:
  truths:
    - "template_tasks table has due_time_offset_minutes column"
    - "template_tasks table has start_time and end_time columns"
    - "useTemplates mutations include new scheduling fields"
  artifacts:
    - path: "supabase/migrations/20260201000001_add_template_task_scheduling.sql"
      provides: "Migration adding scheduling columns to template_tasks"
      min_lines: 30
    - path: "src/integrations/supabase/types.ts"
      provides: "TemplateTask type with scheduling fields"
      contains: "due_time_offset_minutes"
    - path: "src/hooks/useTemplates.ts"
      provides: "Updated mutations passing scheduling fields"
      contains: "due_time_offset_minutes"
  key_links:
    - from: "src/hooks/useTemplates.ts"
      to: "template_tasks"
      via: "INSERT/UPDATE mutations"
      pattern: "due_time_offset_minutes|start_time|end_time"
---

<objective>
Add scheduling columns to template_tasks table to support per-task due times and time blocks.

Purpose: Enable templates to specify when tasks are due (time of day) and optional time blocks for scheduled activities.
Output: Database schema supports template scheduling, TypeScript types updated, mutations ready for UI.
</objective>

<execution_context>
@/Users/haokunyang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haokunyang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-template-scheduling/25-RESEARCH.md
@.planning/phases/24-custom-task-scheduling/24-01-SUMMARY.md
@supabase/migrations/20260119000001_template_preassignment.sql
@src/hooks/useTemplates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database migration for template scheduling columns</name>
  <files>supabase/migrations/20260201000001_add_template_task_scheduling.sql</files>
  <action>
Create migration file that:
1. Adds three new columns to template_tasks table:
   - due_time_offset_minutes INTEGER DEFAULT NULL (minutes from midnight, 0-1439)
   - start_time TEXT DEFAULT NULL (12-hour format like "1:00 PM")
   - end_time TEXT DEFAULT NULL (12-hour format like "2:00 PM")

2. Adds helpful comments explaining column usage:
   - due_time_offset_minutes: Minutes from midnight on (assign_date + day_offset) when task is due
   - start_time: Optional time block start
   - end_time: Optional time block end

3. Creates index on template_id if not exists (optimization for template task queries)

4. Uses IF NOT EXISTS pattern for columns (idempotent migration)

Follow Phase 24 migration pattern (20260131000001_add_scheduling_columns.sql) for column addition syntax.
Use same TEXT type for start_time/end_time as task_instances table for consistency.
  </action>
  <verify>
Run: supabase db reset --local
Check: supabase migration list shows new migration
Query: SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'template_tasks' AND column_name IN ('due_time_offset_minutes', 'start_time', 'end_time')
All three columns should appear with correct types.
  </verify>
  <done>Migration file exists, adds three columns to template_tasks, migration applies successfully to local database</done>
</task>

<task type="auto">
  <name>Task 2: Push migration, regenerate types, and update useTemplates mutations</name>
  <files>src/integrations/supabase/types.ts, src/hooks/useTemplates.ts</files>
  <action>
1. Push migration to remote:
   supabase db push

2. Regenerate TypeScript types:
   supabase gen types typescript --local > src/integrations/supabase/types.ts

3. Update useTemplates.ts mutations to include new fields:
   - createTemplateMutation: Add due_time_offset_minutes, start_time, end_time to taskInserts map
   - updateTemplateMutation: Add same fields to taskInserts map
   - Ensure TemplateTask interface at top of file includes these fields (should auto-update from types)

4. Verify TypeScript compilation:
   npm run type-check

Pattern: Follow existing taskInserts mapping (lines 106-113, 164-171). Add new fields alongside existing title, description, duration_minutes, day_offset, sort_order.
  </action>
  <verify>
Run: npm run type-check (should pass with 0 errors)
Check: src/integrations/supabase/types.ts includes new columns in Tables<'template_tasks'> type
Check: src/hooks/useTemplates.ts includes new fields in both create and update mutations
  </verify>
  <done>Types regenerated with new columns, useTemplates mutations pass all new fields to database, TypeScript compiles without errors</done>
</task>

</tasks>

<verification>
1. Migration file exists and adds three columns
2. supabase migration list shows migration as applied
3. Database schema query confirms columns exist with correct types
4. TypeScript types include new fields on TemplateTask
5. useTemplates mutations map new fields in taskInserts
6. npm run type-check passes with 0 errors
</verification>

<success_criteria>
- template_tasks table has due_time_offset_minutes, start_time, end_time columns
- TypeScript types reflect new schema
- useTemplates hook mutations include new fields
- No type errors, migrations apply cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/25-template-scheduling/25-01-SUMMARY.md`
</output>
