---
phase: 19-student-dashboard-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/student/StudentHome.tsx
  - src/pages/CoachSettings.tsx
autonomous: true

must_haves:
  truths:
    - "Student dashboard shows three boxes side-by-side on desktop"
    - "Student dashboard stacks boxes vertically on mobile"
    - "Each box has colored left border, white background, drop shadow, rounded corners"
    - "Each box header shows bold text only (no icons)"
    - "Empty states show muted text messages"
    - "Coach settings page has no Privacy & Security section"
  artifacts:
    - path: "src/pages/student/StudentHome.tsx"
      provides: "Three-column dashboard layout with styled boxes"
      contains: "lg:grid-cols-3"
    - path: "src/pages/CoachSettings.tsx"
      provides: "Settings page without security features"
      not_contains: "Privacy & Security"
  key_links:
    - from: "src/pages/student/StudentHome.tsx"
      to: "Tailwind grid classes"
      via: "responsive grid layout"
      pattern: "grid-cols-1.*lg:grid-cols-3"
---

<objective>
Restructure student dashboard to display three consistent boxes (My Groups, Tasks to Do, Coach's Notes) in a 3-column desktop layout with proper visual styling. Also remove the placeholder Privacy & Security section from coach settings.

Purpose: Improve student dashboard visual hierarchy and remove non-functional security features from coach settings
Output: Updated StudentHome.tsx with responsive 3-column layout and styled boxes; CoachSettings.tsx without Privacy & Security section
</objective>

<execution_context>
@/Users/haokunyang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haokunyang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-student-dashboard-layout/19-CONTEXT.md
@.planning/phases/19-student-dashboard-layout/19-RESEARCH.md
@src/pages/student/StudentHome.tsx
@src/pages/CoachSettings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update StudentHome.tsx grid layout and box styling</name>
  <files>src/pages/student/StudentHome.tsx</files>
  <action>
Update the student dashboard layout in StudentHome.tsx:

1. **Change grid layout (line ~531):**
   - Current: `grid grid-cols-1 gap-6`
   - New: `grid grid-cols-1 lg:grid-cols-3 gap-3`
   - This makes 3 columns on large screens, stacked on mobile
   - Reduced gap from 6 to 3 (tight spacing per CONTEXT.md: 8-12px = gap-2 or gap-3)

2. **Update card styling and headers for all three cards** (My Groups ~533, Coach's Notes ~638, Tasks to Do ~722):
   - Keep existing `border-l-4` and color classes
   - Add: `bg-white shadow-sm rounded-lg` (white background, subtle drop shadow, rounded corners)
   - Remove any existing border class that conflicts (keep only left border)
   - **Remove icons from CardTitle** - per CONTEXT.md requirement "Bold title only for headers (no icons)":
     - Remove Users icon from My Groups header
     - Remove StickyNote icon from Coach's Notes header
     - Remove CheckSquare icon from Tasks to Do header
     - Remove the `flex items-center gap-2` from CardTitle className (no longer needed without icons)
     - Remove unused icon imports from the top of the file

   Each card header should become simply:
   ```tsx
   <CardTitle className="font-bold text-base">My Groups</CardTitle>
   ```

   Each card should have classes like:
   ```
   Card className="border-l-4 border-l-blue-500 bg-white shadow-sm rounded-lg"
   ```

3. **Ensure equal height boxes:**
   - The grid with `lg:grid-cols-3` naturally makes equal-width columns
   - Tailwind CSS Grid items stretch to fill height by default (align-items: stretch) - no additional class needed

4. **Verify empty state text:**
   - My Groups empty (line ~629): "You haven't joined any groups yet..." - already muted, OK
   - Coach's Notes empty (line ~664): "No notes yet" - already muted, OK
   - Tasks to Do empty (line ~742): "No tasks for today" - already muted, OK

Note: The three main cards are currently ordered: My Groups, Coach's Notes, Tasks to Do. Keep this order. The "Coming Up" and "Empty State" cards below the main grid should remain unchanged.
  </action>
  <verify>
Run `npm run build` - should complete without errors

Programmatic verification:
- `grep -n 'lg:grid-cols-3' src/pages/student/StudentHome.tsx` - should return at least one match showing the responsive grid
- `grep -c 'bg-white shadow-sm rounded-lg' src/pages/student/StudentHome.tsx` - should return 3 (one per card)

Visual verification (when running `npm run dev`):
- Desktop (>1024px): 3 boxes side-by-side
- Mobile (<1024px): 3 boxes stacked vertically
- Each box has colored left border, white background, subtle shadow, rounded corners
  </verify>
  <done>
StudentHome.tsx displays three boxes in responsive grid layout with specified styling (white bg, shadow-sm, rounded-lg, colored left borders, tight spacing)
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Privacy & Security section from CoachSettings.tsx</name>
  <files>src/pages/CoachSettings.tsx</files>
  <action>
Remove the entire Privacy & Security Card section from CoachSettings.tsx:

1. **Delete lines 206-241** (the entire Privacy & Security Card):
   ```tsx
   {/* Privacy Section */}
   <Card className="border-border">
     <CardHeader>
       ...Privacy & Security...
     </CardHeader>
     <CardContent className="space-y-4">
       <Button...>Change Password</Button>
       <Button...>Two-Factor Authentication</Button>
       <Button...>Download My Data</Button>
     </CardContent>
   </Card>
   ```

2. **Remove unused Shield import** from line 2:
   - Current: `import { User, Shield, LogOut, Loader2, Globe } from "lucide-react";`
   - New: `import { User, LogOut, Loader2, Globe } from "lucide-react";`

3. **Ensure layout remains clean:**
   - The remaining sections (Profile, Timezone, Sign Out) should flow naturally
   - No blank space or layout issues after removal
  </action>
  <verify>
Run `npm run build` - should complete without errors
Run `grep -n "Privacy" src/pages/CoachSettings.tsx` - should return nothing
Run `grep -n "Shield" src/pages/CoachSettings.tsx` - should return nothing
  </verify>
  <done>
CoachSettings.tsx has no Privacy & Security section, no Shield import, and layout flows cleanly with remaining sections (Profile, Timezone, Sign Out)
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. TypeScript compilation succeeds (no unused import warnings)
3. StudentHome.tsx contains `lg:grid-cols-3` for responsive layout
4. StudentHome.tsx cards have `bg-white shadow-sm rounded-lg` styling
5. CoachSettings.tsx does not contain "Privacy", "Shield", "Change Password", "Two-Factor", or "Download My Data"
</verification>

<success_criteria>
- Student dashboard displays 3 boxes side-by-side on desktop (lg breakpoint)
- Student dashboard stacks boxes vertically on mobile
- All three boxes have: colored left border, white background, subtle drop shadow, rounded corners
- All three box headers show bold text only (no icons)
- Empty states show muted gray text
- Coach settings has no Privacy & Security section
- Build passes, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-student-dashboard-layout/19-01-SUMMARY.md`
</output>
