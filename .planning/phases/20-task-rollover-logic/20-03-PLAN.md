---
phase: 20-task-rollover-logic
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - src/pages/student/StudentHome.tsx
autonomous: true

must_haves:
  truths:
    - "Student sees today's tasks with both completed and pending mixed in place"
    - "Student sees overdue section below today's tasks"
    - "Overdue section collapses after 5 tasks with 'and X more overdue...' message"
    - "Student sees yesterday's completed section (collapsed by default)"
    - "Yesterday section can be dismissed via X button"
    - "Empty state shows 'All done!' when no today's tasks"
  artifacts:
    - path: "src/pages/student/StudentHome.tsx"
      provides: "Student task view with rollover sections"
      contains: ["useTaskRollover", "useSessionDismissal"]
  key_links:
    - from: "src/pages/student/StudentHome.tsx"
      to: "src/hooks/useTaskRollover.ts"
      via: "import and use hook"
      pattern: "useTaskRollover"
    - from: "src/pages/student/StudentHome.tsx"
      to: "src/hooks/useSessionDismissal.ts"
      via: "import and use hook"
      pattern: "useSessionDismissal"
---

<objective>
Integrate task rollover logic into StudentHome with proper section layout.

Purpose: Display tasks in three distinct sections (Today, Overdue, Yesterday) with proper ordering, collapsing, and dismissal behavior.

Output: Updated StudentHome.tsx with task rollover sections per CONTEXT.md specifications.
</objective>

<execution_context>
@/Users/haokunyang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haokunyang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-task-rollover-logic/20-CONTEXT.md
@.planning/phases/20-task-rollover-logic/20-RESEARCH.md
@src/pages/student/StudentHome.tsx
@src/hooks/useTaskRollover.ts
@src/hooks/useSessionDismissal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor fetchTasks to get all relevant dates</name>
  <files>src/pages/student/StudentHome.tsx</files>
  <action>
Modify `fetchTasks` function to fetch tasks for yesterday as well:

1. Get `yesterdayDateString` from `useTimezone()` (now available from Plan 01)
2. Update the query to include:
   - All tasks for today (any status)
   - All pending tasks from before today (overdue)
   - All completed tasks from yesterday (for yesterday section)
3. The current query already does most of this with `.lte("scheduled_date", today)`, but we need to also include yesterday's completed tasks

Modify the filter logic in `fetchTasks`:
- Remove the filter that hides yesterday's completed tasks
- Let `useTaskRollover` handle the categorization instead
- Store raw tasks in state, let hooks categorize

Also update the Supabase query to explicitly include yesterday's completed tasks:
```sql
.or(`scheduled_date.eq.${today},scheduled_date.eq.${yesterday},and(scheduled_date.lt.${today},status.eq.pending)`)
```
  </action>
  <verify>
Run dev server: `npm run dev`
Check console for fetch logs showing tasks from today and yesterday.
  </verify>
  <done>
fetchTasks retrieves today's tasks (all statuses), overdue tasks (pending, before today), and yesterday's completed tasks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate useTaskRollover and useSessionDismissal hooks</name>
  <files>src/pages/student/StudentHome.tsx</files>
  <action>
Add hooks and restructure the task display:

1. Import hooks:
   ```typescript
   import { useTaskRollover } from "@/hooks/useTaskRollover";
   import { useSessionDismissal } from "@/hooks/useSessionDismissal";
   ```

2. Use hooks in component:
   ```typescript
   const { today, overdue, yesterdayCompleted } = useTaskRollover(tasks);
   const { isDismissed, dismiss, reset } = useSessionDismissal();
   ```

3. Update progress calculation to use `today` array (not all tasks):
   ```typescript
   const completedCount = today.filter(t => t.status === "completed").length;
   const totalCount = today.length;
   ```

4. Restructure the task Card to show three sections:
   - **Today's Tasks** header (existing, but use `today` array)
   - **Overdue** section (new, below today)
   - **Yesterday's completed** section (new, below overdue)

5. Section order per CONTEXT.md: Today -> Overdue -> Yesterday

6. Empty state handling per CONTEXT.md:
   - If no today's tasks but has overdue: show overdue only (no "All done!")
   - If only yesterday's completed: show that section alone
   - If nothing at all: show "All done!" message
  </action>
  <verify>
Run dev server, check that tasks appear in correct sections.
  </verify>
  <done>
Tasks are categorized into three sections using hooks. Progress shows today's tasks only.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build Overdue and Yesterday sections with UI per CONTEXT.md</name>
  <files>src/pages/student/StudentHome.tsx</files>
  <action>
Implement the section UI per CONTEXT.md decisions:

**Overdue Section:**
1. Header: "Overdue" with count badge
2. Show first 5 tasks
3. If more than 5, use Radix Collapsible:
   ```tsx
   <Collapsible>
     <CollapsibleTrigger>
       and {overdue.length - 5} more overdue...
     </CollapsibleTrigger>
     <CollapsibleContent>
       {/* remaining tasks */}
     </CollapsibleContent>
   </Collapsible>
   ```
4. Each task shows original due date (per CONTEXT.md)
5. Red/warning styling for overdue indication (already exists)

**Yesterday's Completed Section:**
1. Collapsible by default (starts closed)
2. Header shows summary: "3 tasks completed yesterday" with ChevronDown
3. X button on the right to dismiss (calls `dismiss()`)
4. When expanded: shows all yesterday's completed tasks
5. Read-only (no checkbox interaction - per CONTEXT.md)
6. Hide section entirely if `isDismissed` is true

**Day boundary reset:**
- When day changes (detected by useDayBoundary), call `reset()` to show new yesterday section
- Hook this into the day boundary change via useEffect watching `todayDateString`

**Styling notes:**
- Yesterday section: muted/gray styling (completed tasks)
- Use existing Card component for consistency
- Keep existing overdue red styling
  </action>
  <verify>
1. Run dev server
2. Verify overdue collapses after 5 tasks
3. Verify yesterday section is collapsed by default
4. Verify X button dismisses yesterday section
5. Verify dismissal persists across page refresh (within session)
  </verify>
  <done>
Overdue section collapses after 5 with "and X more" trigger. Yesterday section is collapsible, dismissible via X button, and hidden after dismissal.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Today's tasks show in order with completed tasks mixed in place (not sunk to bottom)
3. Overdue section shows pending tasks from before today, sorted newest-first
4. Overdue collapses after 5 tasks with expandable trigger
5. Yesterday section is collapsed by default, shows summary count
6. X button dismisses yesterday section for the browser session
7. Opening new tab shows same dismissal state (cross-tab sync)
8. Day boundary change resets yesterday dismissal state
</verification>

<success_criteria>
- TASK-01: Completed tasks from yesterday don't appear in today's list
- TASK-02: Each day shows correct tasks with correct completion state
- Section order: Today -> Overdue -> Yesterday
- Overdue tasks show original due date
- Yesterday section is dismissible and persists dismissal per session
- Empty states handled correctly per CONTEXT.md rules
</success_criteria>

<output>
After completion, create `.planning/phases/20-task-rollover-logic/20-03-SUMMARY.md`
</output>
