---
phase: 20-task-rollover-logic
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/hooks/useTaskRollover.ts
  - src/hooks/useSessionDismissal.ts
autonomous: true

must_haves:
  truths:
    - "Tasks are categorized into today, overdue, and yesterday completed sections"
    - "Overdue tasks are sorted newest-first"
    - "Yesterday's completed section can be dismissed for browser session"
    - "Dismissal syncs across tabs in same browser session"
  artifacts:
    - path: "src/hooks/useTaskRollover.ts"
      provides: "Task categorization logic"
      exports: ["useTaskRollover", "CategorizedTasks"]
    - path: "src/hooks/useSessionDismissal.ts"
      provides: "Session-scoped dismissal with cross-tab sync"
      exports: ["useSessionDismissal"]
  key_links:
    - from: "src/hooks/useTaskRollover.ts"
      to: "src/hooks/useTimezone.ts"
      via: "import useTimezone"
      pattern: "useTimezone"
    - from: "src/hooks/useSessionDismissal.ts"
      to: "BroadcastChannel"
      via: "Cross-tab communication"
      pattern: "BroadcastChannel"
---

<objective>
Create task categorization and session dismissal hooks for student task view.

Purpose: Enable proper task section organization (Today, Overdue, Yesterday) with dismissable yesterday section.

Output:
- `useTaskRollover` hook for categorizing tasks
- `useSessionDismissal` hook for cross-tab dismissal state
</objective>

<execution_context>
@/Users/haokunyang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haokunyang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-task-rollover-logic/20-CONTEXT.md
@.planning/phases/20-task-rollover-logic/20-RESEARCH.md
@src/hooks/useTimezone.ts
@src/pages/student/StudentHome.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useSessionDismissal hook</name>
  <files>src/hooks/useSessionDismissal.ts</files>
  <action>
Create a hook for session-scoped dismissal with cross-tab sync per CONTEXT.md:

```typescript
const YESTERDAY_DISMISSED_KEY = 'yesterday-tasks-dismissed';
const BROADCAST_CHANNEL_NAME = 'task-rollover-sync';
```

Hook implementation:
1. `useState` initialized from sessionStorage (try/catch for SSR safety)
2. `useEffect` to set up BroadcastChannel listener:
   - Listen for `{ type: 'YESTERDAY_DISMISSED' }` messages
   - When received: update state + sessionStorage
   - Cleanup: remove listener and close channel
3. `dismiss` callback that:
   - Sets state to true
   - Writes to sessionStorage
   - Broadcasts message to other tabs via BroadcastChannel
4. `reset` callback that:
   - Sets state to false
   - Removes from sessionStorage
   - Broadcasts reset message

Return: `{ isDismissed, dismiss, reset }`

Per CONTEXT.md:
- Dismissal persists for browser session (sessionStorage)
- Shared across tabs (BroadcastChannel)
- Only X button dismisses (dismiss function)
</action>
  <verify>
TypeScript compiles: `npx tsc --noEmit src/hooks/useSessionDismissal.ts`
  </verify>
  <done>
Hook exports `useSessionDismissal` with `isDismissed`, `dismiss`, and `reset` functions. Cross-tab sync works via BroadcastChannel.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create useTaskRollover hook</name>
  <files>src/hooks/useTaskRollover.ts</files>
  <action>
Create a hook that categorizes tasks into sections per CONTEXT.md decisions:

Types:
```typescript
export interface CategorizedTasks {
  today: TaskInstance[];           // scheduled_date === today, any status
  overdue: TaskInstance[];         // scheduled_date < today, status === 'pending'
  yesterdayCompleted: TaskInstance[]; // scheduled_date === yesterday, status === 'completed'
}
```

Hook signature: `useTaskRollover(tasks: TaskInstance[]): CategorizedTasks`

Implementation:
1. Get `todayDateString` and `yesterdayDateString` from `useTimezone()`
2. Use `useMemo` to categorize tasks:
   - today: `task.scheduled_date === todayDateString` (any status)
   - overdue: `task.scheduled_date < todayDateString && task.status === 'pending'`
   - yesterdayCompleted: `task.scheduled_date === yesterdayDateString && task.status === 'completed'`
3. Sort per CONTEXT.md:
   - today: by `created_at` ascending (oldest first, preserve creation order)
   - overdue: by `scheduled_date` descending (newest-first, yesterday's at top)
   - yesterdayCompleted: by `created_at` ascending (preserve order)

Import TaskInstance type from useAssignments or define locally.

IMPORTANT per CONTEXT.md:
- Completed tasks stay in original order position in today section (don't sink to bottom)
- Tasks ordered by creation order (oldest assigned first)
- Overdue sorted newest-first (yesterday's at top)
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit src/hooks/useTaskRollover.ts`
  </verify>
  <done>
Hook exports `useTaskRollover` and `CategorizedTasks` type. Tasks correctly categorized and sorted per CONTEXT.md rules.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no type errors
2. useSessionDismissal stores in sessionStorage (check in DevTools)
3. useTaskRollover correctly categorizes sample task data
4. Cross-tab sync works (dismiss in one tab, check another)
</verification>

<success_criteria>
- Tasks categorized into exactly three sections
- Overdue includes all pending tasks from before today
- Yesterday completed includes only completed tasks from exactly yesterday
- Sorting matches CONTEXT.md specifications
- Session dismissal persists across page refreshes within same session
- Dismissal syncs across multiple browser tabs
</success_criteria>

<output>
After completion, create `.planning/phases/20-task-rollover-logic/20-02-SUMMARY.md`
</output>
