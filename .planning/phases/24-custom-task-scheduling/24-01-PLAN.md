---
phase: 24-custom-task-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260131000001_add_scheduling_columns.sql
  - src/integrations/supabase/types.ts
autonomous: true

must_haves:
  truths:
    - "task_instances table has end_time column for time block end"
    - "task_instances table has assign_date column for visibility date"
    - "task_instances table has start_time column for time block start"
    - "RPC assign_task_to_group accepts and stores start_time, end_time, assign_date"
  artifacts:
    - path: "supabase/migrations/20260131000001_add_scheduling_columns.sql"
      provides: "DB schema changes for scheduling columns"
      contains: "ALTER TABLE task_instances"
    - path: "src/integrations/supabase/types.ts"
      provides: "TypeScript types for new columns"
      contains: "start_time"
  key_links:
    - from: "RPC assign_task_to_group"
      to: "task_instances"
      via: "INSERT with new columns"
      pattern: "assign_date|end_time|start_time"
---

<objective>
Add database columns and update RPC function to support separate assign date, due date, and time blocks.

Purpose: The database must support storing assign_date (when student sees task), start_time, and end_time before the UI can be updated to use them. This is the foundation for all Phase 24 features.

Output: Database migration file, updated RPC function, regenerated TypeScript types.
</objective>

<execution_context>
@/Users/haokunyang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haokunyang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-custom-task-scheduling/24-RESEARCH.md
@src/integrations/supabase/types.ts
@src/hooks/useAssignments.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database migration for scheduling columns</name>
  <files>supabase/migrations/20260131000001_add_scheduling_columns.sql</files>
  <action>
Create a new migration file that:

1. Adds `start_time TEXT` column to task_instances (nullable, for time block start)
2. Adds `end_time TEXT` column to task_instances (nullable, for time block end)
3. Adds `assign_date DATE` column to task_instances (nullable initially for backward compatibility)
4. Backfills `assign_date` from `scheduled_date` for existing records
5. Creates index on `assign_date` for efficient filtering
6. Creates or replaces the `assign_task_to_group` RPC function with signature:
   - p_group_id UUID
   - p_title TEXT
   - p_description TEXT DEFAULT NULL
   - p_assign_date DATE DEFAULT NULL (when student sees task)
   - p_due_date DATE DEFAULT NULL (when task is due - maps to scheduled_date)
   - p_start_time TEXT DEFAULT NULL
   - p_end_time TEXT DEFAULT NULL

The RPC function should:
- Create assignment record
- Loop through group members
- Insert task_instances with assign_date, scheduled_date (due), start_time, end_time
- Return count of created instances

Use SECURITY DEFINER to run with owner privileges. Include auth.uid() check.

Reference the example migration in 24-RESEARCH.md for structure.
  </action>
  <verify>
Run `supabase db diff` or manually verify the migration file contains:
- ALTER TABLE task_instances ADD COLUMN for start_time, end_time, assign_date
- CREATE INDEX on assign_date
- CREATE OR REPLACE FUNCTION assign_task_to_group
  </verify>
  <done>
Migration file exists with all required ALTER TABLE statements and RPC function definition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Push migration and regenerate types</name>
  <files>src/integrations/supabase/types.ts</files>
  <action>
1. Run `npx supabase db push` to apply the migration to the database
2. Run `npx supabase gen types typescript --local > src/integrations/supabase/types.ts` to regenerate TypeScript types
3. Verify the generated types.ts includes:
   - start_time in task_instances Row/Insert/Update
   - end_time in task_instances Row/Insert/Update
   - assign_date in task_instances Row/Insert/Update
   - assign_task_to_group in Functions section with updated parameters

Note: If supabase db push requires linking, use `npx supabase db push --local` or ensure local DB is running.
  </action>
  <verify>
- `npx supabase db push` completes without errors
- `grep -E "start_time|end_time|assign_date" src/integrations/supabase/types.ts` shows the new columns in task_instances
- `grep "assign_task_to_group" src/integrations/supabase/types.ts` shows the function definition
  </verify>
  <done>
types.ts contains start_time, end_time, and assign_date in task_instances table definition.
Database has the new columns and updated RPC function.
  </done>
</task>

</tasks>

<verification>
- [ ] Migration file exists in supabase/migrations/
- [ ] Migration includes ALTER TABLE for all three columns
- [ ] Migration includes assign_task_to_group RPC function
- [ ] types.ts has start_time, end_time, assign_date in task_instances
- [ ] Database accepts the new columns
</verification>

<success_criteria>
The database schema supports:
1. Storing separate assign_date (visibility) and scheduled_date (due date)
2. Storing time blocks with start_time and end_time
3. The assign_task_to_group RPC function accepts all new parameters

TypeScript types are regenerated and include all new fields.
</success_criteria>

<output>
After completion, create `.planning/phases/24-custom-task-scheduling/24-01-SUMMARY.md`
</output>
